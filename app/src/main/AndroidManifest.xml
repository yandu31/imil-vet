<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alejandro.imilvet">
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ImilVET">
        <activity
            android:name="alejandro.imilvet.Galeria"
            android:exported="false" />
        <activity
            android:name="alejandro.imilvet.Camara"
            android:exported="false" />
        <activity
            android:name="alejandro.imilvet.DatosPacientes"
            android:exported="false" />
        <activity
            android:name="alejandro.imilvet.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>